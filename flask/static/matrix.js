//Operations on array buffers or other things that are shaped like matricies

//Return a smaller section of a matrix
//matrix, matrix_x, matrix_y: source matrix & its dimensions
//section_x, section_y, section_w, section_h: the origin point & dimensions of the section
function matrix_section(matrix, matrix_x, matrix_y, section_x, section_y, section_w, section_h){
    //do this
}
